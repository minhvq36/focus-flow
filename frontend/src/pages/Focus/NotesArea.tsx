import React from 'react';

/**
 * NotesArea component - Quick notes during focus session
 */
export default function NotesArea() {
  return (
    <div className="notes-area">
      <textarea placeholder="Add notes..." />
    </div>
  );
}
