package ai

// Suggest handles todo suggestion
type Suggest struct {
}

// Generate suggests todos
func (s *Suggest) Generate(userID string) ([]string, error) {
	// Todo suggestion logic
	return []string{}, nil
}
